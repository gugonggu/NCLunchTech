"use client";
import { useState } from "react";
const categories=["한식","중식","일식","양식","분식","아시아 음식","패스트푸드","카페·간단식","기타"];
export function ManualKakaoRestaurantForm(){const [f,setF]=useState({name:"",category:"한식",address:"",lat:"",lng:"",kakaoUrl:""});const [m,setM]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/restaurants/manual',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const d=await r.json();setM(r.ok?(d.existing?'이미 등록된 식당이에요.':'식당을 등록했어요.'):d.message)}return <form onSubmit={submit} className="rounded-card bg-surface p-4 shadow-card"><p className="font-semibold text-brand-dark">Kakao 장소 URL 수동 추가</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{([['name','상호명'],['address','주소'],['lat','위도'],['lng','경도'],['kakaoUrl','Kakao 장소 URL']] as const).map(([k,l])=><input key={k} required={k!== 'address'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} placeholder={l} className="rounded-control border border-line px-3 py-2"/>)}<select value={f.category} onChange={e=>setF({...f,category:e.target.value})} className="rounded-control border border-line px-3 py-2">{categories.map(c=><option key={c}>{c}</option>)}</select></div><button className="mt-3 rounded-control bg-brand px-3 py-2 font-semibold text-black">수동 등록</button>{m&&<p className="mt-2 text-sm text-ink-muted">{m}</p>}</form>}
